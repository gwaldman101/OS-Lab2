class FCFS:
    cycle = 0
    processes = []
    current_process = 0
    num_processes = 0
    num_terminated = 0
    currently_running = False

    def FCFS(processes, verbose):
        this.processes = processes
        this.num_processes = len(processes)

        output = ''

        while(num_terminated < num_processes):
            output = output + 'Before cycle: ' + cycle + ': '
            for i in range(num_processes):
                current_process = processes[i]

                if current_process.arrival_time < cycle:
                    if process.state == 'running'

                else:
                    pass
